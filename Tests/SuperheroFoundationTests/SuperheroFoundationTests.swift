import XCTest
@testable import SuperheroFoundation

final class SuperheroFoundationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(SuperheroFoundation().text, "Hello, World!")
    }
}
